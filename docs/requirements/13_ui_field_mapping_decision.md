# UI 字段语义映射待确认记录

## 冲突内容

本次 UI 需求将 FAVC、FCVC、NCP、CAEC、CH2O、SCC、FAF、TUE、CALC、MTRANS 分别表述为高热量食物摄入、蔬菜摄入频率、每日进餐次数、两餐之间进食频率、每日饮水情况、热量监测、身体活动频率、电子设备使用情况、饮酒频率和主要交通方式。

现有 `docs/requirements/05_data_dictionary.md` 则明确记录：上述缩写的正式含义、单位、部分 0/1 映射和字符串 `0` 含义尚未获得数据说明或教师确认，界面不得将推测文案定稿。

## 当前处理

- 状态：等待用户或教师确认。
- 原始 CSV、字段名和取值不做任何修改。
- UI 暂时使用“中文候选名 + 原字段名 + 真实观察范围/取值”。
- 0/1 选项显示原值并标注“含义待确认”，不自行映射为“是/否”。
- Age、Height、Weight 的单位使用“疑似”说明，不作为官方单位。
- 该处理不影响模型输入、训练、评估或预测，只影响界面友好文案。

## 待确认决策

确认是否授权按本次 UI 文案固定上述字段含义，并补充 Age、Height、Weight 的单位及所有 0/1、字符串 `0` 的正式中文映射。确认后应集中修改 `src/ui/constants.py` 与数据字典，避免页面分散硬编码。
