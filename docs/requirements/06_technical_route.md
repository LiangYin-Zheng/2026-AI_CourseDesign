# 技术路线

## 技术原则

- Python 3.10，所有命令从指定 Conda 环境运行。
- YAML 驱动配置；`pathlib` 管理路径；`logging` 留存过程；pytest 验证边界。
- pandas/NumPy 完成数据计算，scikit-learn 完成成熟模型和 Pipeline；手写核心算法仅依赖 NumPy。
- 训练、验证、测试严格隔离；相同数据划分服务四个模型；界面只消费稳定服务接口和已验证产物。

## 项目总体流程图

```mermaid
flowchart TD
    A["阶段一：需求与数据只读理解"] --> G1{"阶段一验收"}
    G1 -->|通过| B["阶段二：最小工程骨架"]
    G1 -->|未通过| A
    B --> G2{"阶段二验收"}
    G2 -->|通过| C["阶段三：sklearn 完整版本"]
    G2 -->|未通过| B
    C --> G3{"sklearn 验收"}
    G3 -->|通过| D["阶段四：NumPy 手写算法"]
    G3 -->|未通过| C
    D --> G4{"四模型统一验收"}
    G4 -->|通过| E["阶段五：交互界面与系统集成"]
    G4 -->|未通过| D
    E --> F["日志、PPT、模板报告与最终验收"]
```

## 数据处理流程图

```mermaid
flowchart TD
    A["只读原始 CSV"] --> B["Schema 与质量检查"]
    B --> C["明确目标、排除 id、记录清洗规则"]
    C --> D["先分层切出测试集"]
    D --> E["对剩余数据分层切分训练/验证集"]
    E --> F["训练集拟合 ColumnTransformer"]
    F --> G["训练/验证分别 transform"]
    G --> H["基线与调参仅看训练/验证"]
    H --> I["锁定配置和预处理器"]
    I --> J["测试集仅 transform 和最终评估"]
    J --> K["独立保存处理数据、索引与数据哈希"]
```

## 模型训练和评估流程图

```mermaid
flowchart LR
    A["固定数据划分"] --> B["基线参数"]
    B --> C["训练集拟合"]
    C --> D["验证集评估"]
    D --> E{"改进是否基于验证证据"}
    E -->|是| F["记录候选参数与指标"]
    F --> C
    E -->|否/已锁定| G["冻结最终方案"]
    G --> H["测试集一次最终评估"]
    H --> I["Accuracy/Precision/Recall/F1"]
    H --> J["混淆矩阵/分类报告"]
    H --> K["训练时间/推理时间/复杂度"]
    I --> L["保存结果与文字分析"]
    J --> L
    K --> L
```

## sklearn 与手写算法关系图

```mermaid
flowchart TB
    A["同一原始数据哈希"] --> B["同一训练/验证/测试索引"]
    B --> C["同一预处理原则和标签映射"]
    C --> D1["sklearn LogisticRegression"]
    C --> D2["sklearn MLPClassifier"]
    C --> D3["NumPy Softmax 回归"]
    C --> D4["NumPy 前馈神经网络"]
    D1 --> E["统一预测/概率接口"]
    D2 --> E
    D3 --> E
    D4 --> E
    E --> F["统一指标与耗时协议"]
    F --> G["公平对比与推荐模型"]
```

## 用户预测流程图

```mermaid
flowchart TD
    A["用户进入预测页"] --> B["读取已发布模型元数据和输入 schema"]
    B --> C["呈现实际字段对应控件"]
    C --> D{"必填/范围/类别校验"}
    D -->|失败| E["字段级错误提示并保留输入"]
    E --> C
    D -->|通过| F["使用随模型保存的预处理器 transform"]
    F --> G["预测类别与各类概率"]
    G --> H["显示原始/友好类别、最高概率、模型版本"]
    H --> I["显示简要解释与非医疗诊断声明"]
```

## 数据划分与调参决策

- 初始计划比例：训练 70%、验证 15%、测试 15%，固定 `random_state=42`；阶段二写入 YAML，阶段三通过测试确认每类均覆盖且比例误差合理。
- 第一次分层切分保留 15% 测试集；第二次对余下 85% 按 `15/85` 比例切出验证集，避免直接比例计算错误。
- 若使用交叉验证，仅能在训练部分执行；验证集作为候选方案的统一比较依据，测试集保持封存。
- 所有模型消费持久化的样本索引/划分标识；不得各自重新随机划分。

## 预处理方案候选

- 连续数值列：训练集拟合缺失处理（即使当前无缺失也保留明确策略）和标准化。
- 二元/名义类别：训练集拟合缺失处理与 One-hot；对验证/测试未知类别使用明确策略。
- 序数候选列是否保持连续数值由字段说明和验证实验决定，决策记录在配置与实验元数据中。
- 标签：固定保存原始标签到整数索引的双向映射，展示层可另有友好文本映射。

## 产物与可复现证据

- 每次实验记录：数据哈希、划分索引/哈希、配置快照、代码版本、Python/依赖版本、开始/结束时间、模型、预处理器、标签映射、指标与图表。
- 图表必须有标题、轴、图例、编号候选和对应解释；报告只使用本人运行生成的图表/界面截图。

